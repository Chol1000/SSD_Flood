# Python
__pycache__/
*.py[cod]
*.egg-info/
dist/
build/
.eggs/
*.egg
.env
.venv
env/
venv/

# Jupyter
.ipynb_checkpoints/
*.ipynb_checkpoints

# Large data files — keep out of repo (use DVC or release)
south_sudan_flood_dataset_2011_2025.csv

# Paper — distributed separately
PAPER_South_Sudan_Flood_Prediction.md

# OS
.DS_Store
Thumbs.db

# IDE
.vscode/
.idea/

# Model binary (large — keep or add to Git LFS)
# model/best_model.pkl

# Local snapshot of model artefacts taken before retraining — the previous
# versions are already in git history, so this is a working copy only.
model_backup_pre_v2/
